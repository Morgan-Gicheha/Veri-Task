export const environment = {
  production: true,
  // Production API (live server) - always use this for production builds
  apiUrl: 'https://morgan-gicheha.com'
  
  // Docker internal API (used when both frontend and backend are in Docker)
  // apiUrl: 'http://backend:8080'
  
  // Local development API (for testing)
  // apiUrl: 'http://localhost:8082'
};
